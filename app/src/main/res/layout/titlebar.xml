<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="title"
            type="java.lang.String"/>
    </data>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:background="@drawable/line_bottom_bg">

    <ImageView
        android:id="@+id/titlebar_left"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:padding="15dp"
        android:src="@mipmap/arraw_left_black"
        android:background="@null" />

    <TextView 
        android:id="@+id/titlebar_center"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:gravity="center"
        android:textSize="20sp"
        android:maxEms="13"
        android:lines="1"
        android:ellipsize="end"
        android:text="@{title}"/>

    <ImageView
        android:id="@+id/titlebar_right"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:padding="15dp"
        android:background="@null" 
        android:layout_alignParentRight="true"
        android:visibility="gone"/>
    <ImageView
        android:id="@+id/titlebar_right2"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:padding="15dp"
        android:background="@null" 
        android:layout_toLeftOf="@id/titlebar_right"
        android:visibility="gone"/>
    <TextView 
        android:id="@+id/titlebar_right3"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:textColor="#ffffff"
        android:layout_alignParentRight="true"
        android:textSize="16sp"
        android:paddingRight="15dp"
        android:paddingLeft="15dp"
        android:visibility="gone"
        android:gravity="center"/>
    <com.gsy.translate.view.LoadingProgressBar
        android:id="@+id/titlebar_progressbar"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_alignParentBottom="true"
        android:progressDrawable="@drawable/progressbar"
        android:visibility="gone"/>
</RelativeLayout>
</layout>